[package]
name = "nano-os"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless = "0.8.0"

[profile.dev]
lto = true
codegen-units = 1
panic = "abort"

[profile.release]
panic = "abort"

[[bin]]
name = "kernel"
path = "src/main.rs"

[features]
print_dtb = []
page_allocator_debug = []
